{
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -qE '(^|[|;&])\\s*(npm\\s+(install|i|ci|add|remove|uninstall|update)|bun\\s+(install|add|remove)|yarn\\s+(install|add|remove))' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Wrong package manager: use pnpm for deps (pnpm install/add/remove). Never: npm install, bun install, yarn install.\"}}' || exit 0"
					}
				]
			}
		]
	}
}
